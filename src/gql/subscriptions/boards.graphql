subscription BoardUpdate($code: String!) {
  boardUpdate(code: $code) {
    id
    title
    status
    ticket
    ticketTimer
    userVotes {
      user {
        id
        username
        email
      }
      status
      vote
      admin
    }
  }
}